name: "react ci pipeline"

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "setup node js"
        uses: actions/setup-node@v4
        with:
          node-version: '21'

      - name: "version check"
        run: node --version